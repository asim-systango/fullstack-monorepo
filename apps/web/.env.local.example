# Browser API base — same-origin path; Next rewrites /api/* → API gateway.
NEXT_PUBLIC_API_URL=/api
# Server-only: Nest api-gateway origin used by next.config rewrites.
API_GATEWAY_URL=http://localhost:3001
