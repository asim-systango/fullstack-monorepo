import { PageShell } from '@/components/layout/page-shell';

export default function JobsPage() {
  return (
    <PageShell
      title="Jobs"
      description="Public job listing — accessible to every role and guests."
    >
      <div className="rounded-md bg-surface p-6 shadow-card">
        <p className="text-sm text-secondary">Job list UI will be built next.</p>
      </div>
    </PageShell>
  );
}
