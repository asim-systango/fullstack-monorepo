import { SectionPlaceholder } from '@/components/splitter/section-placeholder';

export default function ExpensesPage() {
  return (
    <SectionPlaceholder
      title="Expenses"
      description="Open a group to add and review shared expenses."
    />
  );
}
