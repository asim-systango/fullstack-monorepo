# @shared/database

Unused stub. Do not put your project schema here.

Entities: `apps/api/src/modules/**/*.entity.ts`  
Migrations: users/auth in `apps/api-gateway/src/database/migrations/`; domain in `apps/api/src/database/migrations/`

Move shared schema pieces here only if a second service needs them.
